apiVersion: v1
kind: Service
metadata:
  name: icap-marketService
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: icap-marketService
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 80